const LoadingSpinner = () => (
	<div className="flex min-h-full items-center justify-center">
		<div className="border-primary-500 size-3xl animate-spin rounded-full border-b-2"></div>
	</div>
);

export default LoadingSpinner;
